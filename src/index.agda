module index where

-- You probably want to start with this module:
import README

-- For a brief presentation of every single module, head over to
import Everything

-- For a list of the subsection of the library compatible with the --safe
-- command line option, you can read the following index:
import Safe.index

-- Otherwise, here is an exhaustive, stern list of all the available modules:
